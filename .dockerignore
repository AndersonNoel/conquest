# Don't copy node_modules — they're installed fresh inside the image.
node_modules

# Exclude runtime data folders — these are bind-mounted from the host
# so data persists across container rebuilds and updates.
data
uploads

# Never bake secrets into the image.
.env

# Not needed inside the container.
*.log
.git
